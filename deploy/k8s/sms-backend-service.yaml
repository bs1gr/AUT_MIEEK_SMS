apiVersion: v1
kind: Service
metadata:
  name: sms-backend
  labels:
    app: sms-backend
spec:
  selector:
    app: sms-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
