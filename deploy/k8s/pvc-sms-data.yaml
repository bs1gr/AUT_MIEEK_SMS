apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sms-data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: "standard"

# Example: mount this PVC at /data in your Deployment to store the SQLite file