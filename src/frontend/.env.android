# Android build environment.
# Leave VITE_API_URL empty — the user enters the server URL on first launch
# via the built-in Server Setup screen (stored in localStorage).
# To bake in a fixed default instead: VITE_API_URL=https://your-server.com/api/v1
VITE_API_URL=
