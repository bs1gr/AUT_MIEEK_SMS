# Frontend environment example
# Copy to .env and adjust as needed

# For native dev on Windows (scripts/RUN.ps1 starts backend and sets VITE_API_URL automatically):
# You can omit this or keep default.
VITE_API_URL=http://localhost:8000/api/v1

# Optional (development only): enable auto-login for faster local testing
# Leave unset/false in production builds. If enabled, credentials must be provided.
# VITE_ENABLE_AUTO_LOGIN=true
# VITE_AUTO_LOGIN_EMAIL=admin@example.com
# VITE_AUTO_LOGIN_PASSWORD=YourSecurePassword123!
