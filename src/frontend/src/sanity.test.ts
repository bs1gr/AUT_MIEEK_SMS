import { describe, it, expect } from 'vitest';

// Simple sanity test for Vitest setup

describe('Sanity check', () => {
  it('should add numbers correctly', () => {
    expect(1 + 1).toBe(2);
  });
});
