═══════════════════════════════════════════════════════════════
  STUDENT MANAGEMENT SYSTEM - DEPLOYMENT INSTRUCTIONS
  Deploy to PC without Git access
═══════════════════════════════════════════════════════════════

🎯 EASIEST METHOD - Download from GitHub (Recommended)
──────────────────────────────────────────────────────

STEP 1: Install Docker Desktop (if not installed)
   → Download: https://www.docker.com/products/docker-desktop
   → Install and start Docker Desktop

STEP 2: Download release ZIP
   → Go to: https://github.com/bs1gr/AUT_MIEEK_SMS/releases
   → Download: Source code (zip) from v1.8.6.1
   OR direct link:
   → https://github.com/bs1gr/AUT_MIEEK_SMS/archive/refs/tags/v1.8.6.1.zip

STEP 3: Extract ZIP
   → Extract to: C:\SMS or any folder you prefer
   → You'll get folder: AUT_MIEEK_SMS-1.8.6.1\

STEP 4: Run automatic setup
   → Open PowerShell in the extracted folder
   → Run: .\SETUP_AFTER_GITHUB_ZIP.ps1
   
   This script automatically:
   ✅ Checks Docker
   ✅ Creates configuration files
   ✅ Cleans old installations
   ✅ Starts the application

STEP 5: Open browser and login
   → The script will show: http://localhost:8080
   → Email: admin@example.com
   → Password: YourSecurePassword123!

STEP 6: ⚠️ Change password immediately!
   → Control Panel → Maintenance → Change Your Password

═══════════════════════════════════════════════════════════════

📦 ALTERNATIVE METHOD - Copy Docker Image (Faster if no internet)
────────────────────────────────────────────────────────────────

FILES TO COPY:
1. SMS-Docker-Image-v1.8.6.1.tar (104 MB)
2. RUN.ps1
3. backend\.env.example
4. SETUP_AFTER_GITHUB_ZIP.ps1 (optional, for auto-setup)

STEPS ON OTHER PC:

1. Install Docker Desktop
   https://www.docker.com/products/docker-desktop

2. Create folder (e.g., C:\SMS) and copy files

3. Open PowerShell in C:\SMS

4. Load Docker image:
   docker load -i SMS-Docker-Image-v1.8.6.1.tar

5. Create config:
   mkdir backend -ErrorAction SilentlyContinue
   Copy-Item backend\.env.example backend\.env

6. Start:
   .\RUN.ps1

STEP 7: Open browser and login
   → URL: http://localhost:8080
   → Email: admin@example.com
   → Password: YourSecurePassword123!

STEP 8: ⚠️ IMPORTANT - Change password immediately!
   → Go to: Control Panel → Maintenance
   → Find "Change Your Password" card (top, teal color)
   → Enter current password: YourSecurePassword123!
   → Set your new secure password

═══════════════════════════════════════════════════════════════
TROUBLESHOOTING:
═══════════════════════════════════════════════════════════════

Problem: Port 8080 already in use
Solution:
   netstat -ano | findstr :8080
   taskkill /F /PID <number shown>

Problem: Docker not starting
Solution:
   → Restart Docker Desktop
   → Wait 30 seconds
   → Try .\RUN.ps1 again

Problem: Container not starting
Solution:
   docker logs sms-app
   (Check the error messages)

Problem: Cannot login
Solution:
   → Make sure backend\.env exists (copy from .env.example)
   → Stop and restart:
     docker stop sms-app
     docker rm sms-app
     .\RUN.ps1

═══════════════════════════════════════════════════════════════
TO STOP THE APPLICATION:
═══════════════════════════════════════════════════════════════

   .\RUN.ps1 -Stop

OR

   docker stop sms-app
   docker rm sms-app

═══════════════════════════════════════════════════════════════
