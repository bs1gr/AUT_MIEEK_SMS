# Enforce consistent line endings in the repository
# All files use LF in the repo (CI runs on Linux with pre-commit hooks)
# Git will auto-convert to CRLF on Windows checkout
* text=auto eol=lf
# Explicitly set LF for all code and script files
*.sh text eol=lf
*.py text eol=lf
*.ps1 text eol=lf
*.bat text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.md text eol=lf
*.ts text eol=lf
*.tsx text eol=lf
*.js text eol=lf
*.jsx text eol=lf
*.json text eol=lf
*.html text eol=lf
*.css text eol=lf
*.txt text eol=lf
*.ini text eol=lf
*.cfg text eol=lf
*.conf text eol=lf
*.toml text eol=lf
## Recommended for artifact handling (optional): use Git LFS for large release artifacts
# Uncomment or add these lines if you enable Git LFS in the repository:
# /dist/** filter=lfs diff=lfs merge=lfs -text
# *.zip filter=lfs diff=lfs merge=lfs -text
# To enable LFS:
#   git lfs install
#   git lfs track "dist/**"
#   git add .gitattributes
#   git commit -m "chore: enable git lfs for release artifacts"
#   git push
installer/SMS_Installer_*.exe filter=lfs diff=lfs merge=lfs -text
