name: CI Monitor Helper - deprecated

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated
        run: |
          echo "This workflow is deprecated. The main CI Failure Monitor now uploads issue API responses as artifacts."
