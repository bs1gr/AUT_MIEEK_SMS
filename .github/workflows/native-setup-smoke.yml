name: Native Setup Smoke Test

on:
  pull_request:
    paths:
      - 'NATIVE.ps1'
      - '.github/workflows/**'

jobs:
  setup-smoke:
    name: Run .\NATIVE.ps1 -Setup (Windows smoke)
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Python 3.11
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          check-latest: true
          cache: 'pip'

      - name: Run native setup (smoke)
        shell: pwsh
        run: |
          pwsh -NoProfile -Command "Set-Location ${{ github.workspace }}; .\infra\scripts\dev\NATIVE.ps1 -Setup"
