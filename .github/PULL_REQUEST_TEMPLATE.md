<!-- If this PR touches `scripts/operator/` files, ensure an operator reviews and adds the `operator-approved` label -->

## Summary

Describe the change in this PR.

## Changes

- What changed, why, and any migration steps.

## Operator checklist (if PR touches `scripts/operator/`)

- [ ] An operator has reviewed these changes
- [ ] The `operator-approved` label has been (or will be) added before merge
- [ ] The operator confirmed there are no unsafe host-level commands being introduced

If your PR does not touch `scripts/operator/`, the above checklist can be ignored.

## Testing

How was this tested? Include commands and environment variables used.

## Notes

Any additional notes for reviewers.
