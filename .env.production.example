# Production Environment Variables Template
# Copy to .env and customize for production deployment

# PostgreSQL Database Configuration (REQUIRED for production)
POSTGRES_USER=sms_user
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE
POSTGRES_DB=student_management

# Backend Configuration
SECRET_KEY=CHANGE_ME_STRONG_SECRET_KEY_HERE
SMS_ENV=production
SMS_EXECUTION_MODE=docker

# CORS Configuration (comma-separated origins)
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Optional: Version tracking
VERSION=latest
BUILD_DATE=2025-01-11
VCS_REF=main

# Note: DATABASE_URL is automatically constructed in docker-compose.prod.yml
# Format: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

# Security Checklist:
# [ ] Change POSTGRES_PASSWORD to strong random password (min 32 chars)
# [ ] Change SECRET_KEY to strong random key (min 32 chars)
# [ ] Update CORS_ORIGINS to match your production domain(s)
# [ ] Never commit this file with real secrets to version control
# [ ] Use docker secrets or vault in orchestrated environments
