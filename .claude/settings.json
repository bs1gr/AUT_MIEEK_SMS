{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo '${tool_input.command}' | grep -q 'npm run dev\\|vite'; then sleep 3 && start 'http://127.0.0.1:5173/' 2>/dev/null || open 'http://127.0.0.1:5173/' 2>/dev/null || true; fi",
            "statusMessage": "Opening browser at http://127.0.0.1:5173/",
            "async": true
          }
        ]
      }
    ]
  }
}
