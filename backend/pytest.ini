[pytest]
minversion = 8.0
addopts = -ra -q --strict-markers
testpaths = tests
asyncio_mode = auto
python_files = test_*.py
markers =
    no_app_context: tests that do not require app context
    slow: tests that take a long time to run
